export interface UploadAttestationProps {
  uid?: string;
  payload?: any;
}

// this is a controlled component that depends on an external state
export function UploadAttestation(props: UploadAttestationProps) {
  return <>upload attestations</>;
}
